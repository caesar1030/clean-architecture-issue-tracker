import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as LabelIconStories from './label-icon.stories';

<Meta of={LabelIconStories} title="component/LabelIcon/Docs" />

# LabelIcon

레이블을 배경색으로 간략히 표기하기 위한 컴포넌트입니다.

LabelIcon 컴포넌트는 다음 props를 가지고 있습니다.

<Controls />

## ColorName

backgroundColor에 색 이름을 입력할 수 있습니다.

<Canvas of={LabelIconStories.ColorName} />

## ColorCode

backgroundColor에 헥사코드를 입력할 수 있습니다.

<Canvas of={LabelIconStories.ColorCode} />
