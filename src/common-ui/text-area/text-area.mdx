import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as TextAreaStories from './text-area.stories';

<Meta of={TextAreaStories} title="component/TextArea/Docs" />

# TextArea

HTML의 \<textarea\>태그를 래핑한 컴포넌트입니다.

TextArea 컴포넌트는 다음 props를 가지고 있습니다.

<Controls />

## Value

사용자 입력값이 있을 경우, 다음과 같습니다.

<Canvas of={TextAreaStories.Value} />

## Empty

사용자 입력값이 없을 경우, 다음과 같습니다.

<Canvas of={TextAreaStories.Empty} />

## Error

error가 빈 값이 아닐 경우, 하단에 에러메세지를 함께 표시합니다.

<Canvas of={TextAreaStories.Error} />
